#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
export ZEROCLAW_CODEX_REASONING_EFFORT="${ZEROCLAW_CODEX_REASONING_EFFORT:-medium}"

exec "$SCRIPT_DIR/zeroclaw-real" "$@"
