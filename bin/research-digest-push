#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
if [[ -f "$HOME/.zeroclaw/service.env" ]]; then
  set -a
  source "$HOME/.zeroclaw/service.env"
  set +a
fi
exec python3 "$SCRIPT_DIR/../research-digest/scripts/push_digest_to_qq.py" "$@"
